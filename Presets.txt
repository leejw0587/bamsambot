embed = discord.Embed(
            title="Error!",
            description=f"Test Error Message.",
            color=0xE02B2B
        )
await context.send(embed=embed)

---

embed = discord.Embed(
            title="TEST",
            description=f"Test Blurple Message",
            color=0x9C84EF
        )
await context.send(embed=embed)

---
